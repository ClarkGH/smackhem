/**
 * Party system manages party-specific logic:
 * - Party formation (party member relationships)
 * - Leader selection
 * - Party-wide behaviors
 * - Party member offsets/positions relative to leader
 *
 * Note: Individual party member state (position, sprite, collision) is managed
 * by the instance system via InstanceCharacter.
 */

// TODO: Implement party system
